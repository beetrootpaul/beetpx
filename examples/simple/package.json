{
  "name": "beetpx-example-simple",
  "version": "0.0.0",
  "private": true,
  "author": "Beetroot Paul",
  "license": "MIT",
  "scripts": {
    "start": "beetpx dev --open --htmlTitle \"[dev] Simple | BeetPx example\"",
    "build": "beetpx build --htmlTitle \"Simple | BeetPx example\"",
    "prod": "npm run build && beetpx preview --open",
    "zip": "npm run build && beetpx zip",
    "format": "prettier --ignore-path .gitignore --write src/ README.md",
    "tsc": "tsc",
    "tsc:watch": "tsc --watch"
  },
  "devDependencies": {
    "@beetpx/beetpx": "file:../../"
  }
}
